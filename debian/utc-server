USER="utc"
case "$1" in
    start)
	log_action_begin_msg "Starting UTC"
	su ${USER} -c "/opt/utc/start_server.sh"
	;;

    stop)
	log_action_begin_msg "Stopping UTC"
	su ${USER} -c "/opt/utc/stop_server.sh"
	;;

    restart)
        $0 stop
    	$0 start
    ;;
esac
